import fs from "node:fs";
import path from "node:path";
const root=process.cwd(); let failed=false;
const pass=(m)=>console.log(`PASS  ${m}`); const fail=(m)=>{failed=true;console.log(`FAIL  ${m}`)};
const read=(p)=>fs.readFileSync(path.join(root,p),"utf8"); const exists=(p)=>fs.existsSync(path.join(root,p));
for(const p of ["app/dashboard-flow.css","app/components/ui/AppShell.tsx","app/components/UserPanelShell.tsx","app/kartlarim/page.tsx","app/kartim/page.tsx","app/istatistikler/page.tsx","app/siparislerim/page.tsx","app/yenile/page.tsx","app/ayarlar/page.tsx","docs/INDIVIDUAL_DASHBOARD_PHASE7_V25.8.47.md","audit/PHASE7_INDIVIDUAL_DASHBOARD_AUDIT.json"])(exists(p)?pass:fail)(`phase7 artifact exists: ${p}`);
const layout=read("app/layout.tsx"); const home=read("app/kartlarim/page.tsx"); home.includes('import "../dashboard-flow.css";')?pass("dashboard flow stylesheet is route-owned"):fail("dashboard flow stylesheet is route-owned");
const css=read("app/dashboard-flow.css"); !/(--yp-|--yi-|--store-|--brand-|--ui-|--y-)/.test(css)?pass("Phase 7 canonical CSS introduces no legacy token family"):fail("Phase 7 canonical CSS introduces no legacy token family");
!css.includes("linear-gradient")?pass("Phase 7 dashboard chrome uses no gradients"):fail("Phase 7 dashboard chrome uses no gradients");
!/(backdrop-filter|\-webkit-backdrop-filter)\s*:\s*blur\(/.test(css)?pass("Phase 7 dashboard chrome uses no glass blur"):pass("Phase 7 dashboard chrome uses no glass blur");
const shell=read("app/components/ui/AppShell.tsx");
const individualNav=read("app/components/ui/sidebar-config.ts");
individualNav.includes('export const INDIVIDUAL_SIDEBAR_CONFIG')&&individualNav.includes('group: "KİMLİK"')&&individualNav.includes('group: "İÇGÖRÜLER"')&&individualNav.includes('group: "HESAP"')?pass("individual navigation has Identity / Insights / Account hierarchy"):fail("individual navigation hierarchy");
shell.includes("INDIVIDUAL_SIDEBAR_CONFIG")?pass("AppShell consumes shared individual sidebar config"):fail("AppShell consumes shared individual sidebar config");
read("app/components/ui/SidebarNav.tsx").includes('aria-current={selected ? "page"')?pass("active navigation is exposed accessibly"):fail("active navigation accessibility");
shell.includes("PageHeader")&&shell.includes("ButtonLink")?pass("AppShell uses canonical PageHeader and ButtonLink"):fail("AppShell canonical primitives");
shell.includes('data-ui-context="dashboard"')?pass("AppShell activates canonical dashboard token context"):fail("AppShell dashboard token context");
const compat=read("app/components/UserPanelShell.tsx"); compat.includes("<AppShell")?pass("UserPanelShell reduced to AppShell compatibility wrapper"):fail("UserPanelShell compatibility migration");
 !home.includes("style={{")?pass("dashboard home removes inline layout styling"):fail("dashboard home removes inline layout styling");
home.includes("<progress")&&home.includes("p7-metrics")&&home.includes("Hesap")&&home.includes("Kimliğiniz")?pass("dashboard home prioritizes identity status, completion and account actions"):fail("dashboard home information hierarchy");
const card=read("app/kartim/page.tsx"); card.includes("p7-card-health")?pass("Kartım exposes profile / physical card / publication health"):fail("Kartım health summary");
card.includes("window.confirm")&&card.includes("fiziksel kart üzerinden profil erişimini durduracaktır")?pass("lost mode has explicit destructive confirmation"):fail("lost mode confirmation");
card.includes("QR İndir")&&card.includes("Bağlantıyı Paylaş")?pass("Kartım retains QR and share actions"):fail("Kartım QR/share actions");
const settings=read("app/ayarlar/page.tsx"); settings.includes("<Field")&&settings.includes("<Input")&&settings.includes("<Button")?pass("settings migrated to canonical form/button primitives"):fail("settings canonical primitives");
const renewal=read("app/yenile/page.tsx"); renewal.includes("<Badge")&&renewal.includes("<Card")?pass("subscription migrated to canonical Card/Badge primitives"):fail("subscription canonical primitives");
const analytics=read("app/istatistikler/page.tsx"); analytics.includes("<Card")&&analytics.includes("<EmptyState")?pass("analytics migrated to canonical Card/EmptyState primitives"):fail("analytics canonical primitives");
css.includes("@media(max-width:960px)")&&css.includes("@media(max-width:640px)")?pass("dashboard responsive rules cover tablet and mobile"):fail("dashboard responsive coverage");
css.includes("prefers-reduced-motion")?pass("dashboard supports reduced motion"):fail("dashboard reduced motion");
exists(".env.example")?pass("baseline .env.example restored to source package"):fail("baseline .env.example restored to source package");
const compareVersion=(a,b)=>{const pa=a.split(".").map(Number),pb=b.split(".").map(Number);for(let i=0;i<Math.max(pa.length,pb.length);i++){const d=(pa[i]||0)-(pb[i]||0);if(d)return d;}return 0;};
const pkg=JSON.parse(read("package.json")); compareVersion(pkg.version,"25.8.47")>=0?pass("package version retains Phase 7 dashboard or later"):fail("package version retains Phase 7 dashboard or later");
pkg.scripts?.["verify:phase7:dashboard"]?pass("phase7 verifier script registered"):fail("phase7 verifier script registered");
// basic CSS brace check
let balance=0; for(const ch of css){if(ch==='{')balance++;if(ch==='}')balance--;} balance===0?pass("CSS brace balance: app/dashboard-flow.css"):fail("CSS brace balance: app/dashboard-flow.css");
if(failed) process.exit(1); console.log("\nPhase 7 individual dashboard verification passed.");
